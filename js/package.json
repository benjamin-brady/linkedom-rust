{
  "name": "@linkedom-rust/dom",
  "version": "0.1.0",
  "description": "TypeScript wrapper for the linkedom-rust WASM DOM library",
  "type": "module",
  "main": "./index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    }
  },
  "scripts": {
    "build:wasm": "wasm-pack build .. --target web --out-dir ../pkg",
    "build:ts": "tsc --noEmit",
    "build": "npm run build:wasm && tsc --declaration --outDir .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "notes": [
    "Run 'npm run build:wasm' first to generate ../pkg from Rust source.",
    "Requires: rustup + wasm-pack + wasm32-unknown-unknown target.",
    "See WASM build blocker note in docs/superpowers/plans/2026-05-05-linkedom-rust-wasm-implementation.md"
  ]
}
